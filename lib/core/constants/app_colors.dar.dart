import 'package:flutter/material.dart';

/// Central place for PharmaTrack color palette
class AppColors {
  // 🌤️ Light Theme Base
  static const Color primary = Color(0xFF2E7D32); // Green shade (brand tone)
  static const Color secondary = Color(0xFF81C784); // Lighter green accent
  static const Color background = Color(0xFFF9FAFB); // Soft white-gray
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);

  // Optional Dark Theme (for future use)
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color textDark = Color(0xFF212121);
  static const Color textLight = Color(0xFF9E9E9E);
}
